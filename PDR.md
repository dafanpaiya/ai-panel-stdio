# AI Panel Studio — 产品需求文档 (PDR)

## 1. 产品概述

### 1.1 产品定位

AI Panel Studio 是一款**本地运行的 AI 圆桌讨论 Web 应用**。用户输入任意讨论话题并指定参会专家人数，系统调用大模型动态生成主持人与专家嘉宾阵容，随后在演播厅中驱动一场由 AI 主导、实时推进的多角色圆桌讨论。

### 1.2 目标用户

| 用户类型 | 核心诉求 |
|----------|----------|
| **知识工作者** | 获得不同视角的深度分析与争辩摘要，突破个人思维局限 |
| **产品/决策人员** | 发现盲点、检验假设，提升决策质量 |
| **内容创作者** | 获得可录制、可回放的"虚拟专家访谈"作为内容素材 |

### 1.3 解决的痛点

| 痛点 | 解决方案 |
|------|----------|
| **视角单一** | 多专家角色从不同立场出发，自动产生分歧与补充 |
| **异步割裂** | 所有 AI 角色共享同一讨论上下文，在统一 transcript 中协作 |
| **过程不可见** | 专家状态小窗实时展示思考摘要，共识/分歧增量可见 |
| **议题管理混乱** | 每个讨论独立隔离，支持并行管理多个议题 |

---

## 2. 功能需求

### 2.1 首页 — 讨论列表

- 展示所有讨论列表，每项显示：话题、状态（草稿/阵容就绪/进行中/已暂停/已结束）、创建时间、专家人数
- 用户可**发起新讨论**
- 用户可**加入观察**已有讨论（点击进入演播厅）
- 已结束的讨论可查看回放/transcript，可删除

### 2.2 阵容生成向导

1. 用户输入讨论话题
2. 用户指定专家人数（默认 4 人）
3. 用户设置最大讨论轮次（系统默认 12，可调整）
4. 系统调用大模型动态生成：
   - 1 名主持人（姓名、职业、title、立场、专属颜色）
   - N 名专家（每人：姓名、职业、title、立场、专属颜色标识）
5. 以卡片阵列展示生成结果
6. 用户可对阵容进行编辑：
   - 编辑任意嘉宾的姓名/职业/title/立场/颜色
   - 删除不想要的专家
   - 手动新增自定义专家
   - 单独重新生成某一位专家
   - 全部重新生成
7. 用户确认阵容后进入演播厅

### 2.3 演播厅模式

#### 2.3.1 讨论流程

1. **开场**：主持人进行开场白，介绍话题与嘉宾
2. **主循环**：专家自主发言，主持人按条件触发介入
3. **收尾**：主持人进行自然语言总结

#### 2.3.2 专家发言机制（两步管道）

**第一步 — 调度（Scheduler）：** 轻量 LLM 调用，仅决定"谁发言 + 发言类型"，不生成发言内容。

- 发言类型：`new_point`（新观点）、`supplement`（补充）、`rebuttal`（反驳）
- 约束规则：
  - 同一专家连续发言不超过 2 次
  - 优先选择最近未发言的专家
  - 禁止机械式轮流发言
  - 每次发言控制在 1-2 句自然语言

**第二步 — 发言生成（Speaker）：** 该专家的系统 prompt + 完整 transcript → 生成 1-2 句自然发言。

#### 2.3.3 主持人触发条件

- **开场触发**：讨论启动时，主持人独白开场
- **轮次触发**：每 3-4 轮专家发言后，主持人介入串联/追问
- **冷场触发**：检测到长时间无新发言时，主持人追问引导
- **跑题触发**：LLM 检测到讨论偏离话题时，主持人引导回正轨
- **用户触发**：用户插入追问 → 主持人转述并引导讨论
- **收尾触发**：达到最大轮次或用户手动结束时，主持人自然语言总结

#### 2.3.4 用户控制权

- 暂停 / 继续讨论
- 插入追问或调整话题方向
- 手动结束讨论

### 2.4 专家状态小窗

每位专家拥有独立状态小窗，实时显示：

| 状态 | 含义 | 显示内容 |
|------|------|----------|
| `idle`（待机） | 等待被调度 | 当前关注点或上次发言摘要 |
| `preparing`（准备发言） | 被调度选中，正在生成发言 | "正在组织观点…" |
| `speaking`（发言中） | 发言已生成，正在展示 | 当前发言内容摘要 |

> **重要约束**：仅展示公开思考摘要，不暴露真实的隐藏 chain-of-thought。

### 2.5 实时共识与分歧

- 每次专家发言后，增量调用 LLM 检查"本条发言是否引入新共识/分歧"
- 若有则增量更新，SSE 广播 `insight_update` 事件
- 共识与分歧区域在前端实时刷新，不等讨论结束

### 2.6 现场 Transcript

- 实时显示所有发言内容（主持人 + 专家）
- 每条发言显示：发言人姓名、职业/title
- 以发言人的专属颜色色块区分
- **不显示**"举手"等内部调度事件
- Transcript 区域独立滚动

### 2.7 讨论总结

- 主持人以自然语言进行总结
- 回顾核心观点、主要共识、关键分歧
- **禁止**将 JSON 原文或结构化数据直接显示到页面上

---

## 3. 非功能需求

### 3.1 技术架构

| 维度 | 方案 |
|------|------|
| 前端 | React + TypeScript |
| 后端 | Python FastAPI |
| 通信 | REST API + SSE（Server-Sent Events） |
| 数据库 | SQLite（本地单文件） |
| 大模型 | DeepSeek V4 Flash（OpenAI 兼容协议） |
| API Key | 仅存放于后端环境变量，不暴露到浏览器 |

### 3.2 多讨论并行隔离

- 每个讨论独立的 DiscussionEngine 实例
- 独立的 asyncio.Task、事件队列、SQLite 连接
- 不同讨论的状态、transcript、共识与分歧完全隔离

### 3.3 响应式布局

- 页面 UI 为中文
- 各区域在自身容器内独立滚动，不依赖整个页面滚动
- 三个断点适配：超宽屏（≥1440px）、普通桌面（1024-1439px）、窄屏（<1024px）
- 每个断点下合理分配面板区域

### 3.4 数据持久化

- SQLite 存储讨论元数据、嘉宾配置、transcript、共识/分歧记录
- 专家实时状态同时写 SQLite 以便崩溃恢复
- 支持历史讨论回放查看

---

## 4. 数据模型

### 4.1 核心实体

```
discussions ——< panelists ——< utterances
    |
    +——< insights
    |
    +——< panelist_states
```

### 4.2 表结构

#### discussions（讨论会话）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT PK | UUID |
| topic | TEXT | 讨论话题 |
| max_rounds | INTEGER | 最大轮次（默认 12） |
| status | TEXT | draft / lineup_ready / running / paused / ended |
| created_at | TEXT | ISO 8601 |
| updated_at | TEXT | ISO 8601 |

#### panelists（嘉宾，含主持人）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT PK | UUID |
| discussion_id | TEXT FK | 所属讨论 |
| role | TEXT | moderator / expert |
| name | TEXT | 姓名 |
| occupation | TEXT | 职业 |
| title | TEXT | 头衔/职称 |
| stance | TEXT | 立场描述 |
| color | TEXT | 专属颜色 hex |
| sort_order | INTEGER | 排列顺序 |
| created_at | TEXT | ISO 8601 |

#### utterances（发言记录）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT PK | UUID |
| discussion_id | TEXT FK | 所属讨论 |
| panelist_id | TEXT FK | 发言人 |
| type | TEXT | opening / main / supplement / rebuttal / moderator_interjection / closing |
| content | TEXT | 自然语言发言内容 |
| round | INTEGER | 轮次号 |
| seq | INTEGER | 全局序号 |
| created_at | TEXT | ISO 8601 |

#### insights（共识/分歧）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | TEXT PK | UUID |
| discussion_id | TEXT FK | 所属讨论 |
| type | TEXT | consensus / divergence |
| content | TEXT | 描述内容 |
| source_utterance_ids | TEXT | JSON array: 关联发言 ID |
| version | INTEGER | 版本号（增量合并用） |
| created_at | TEXT | ISO 8601 |
| updated_at | TEXT | ISO 8601 |

#### panelist_states（专家实时状态）

| 字段 | 类型 | 说明 |
|------|------|------|
| panelist_id | TEXT PK FK | 关联 panelists.id |
| discussion_id | TEXT FK | 所属讨论 |
| status | TEXT | idle / preparing / speaking |
| focus | TEXT | 当前关注点 / 公开思考摘要 |
| updated_at | TEXT | ISO 8601 |

---

## 5. 技术决策记录

| # | 决策项 | 选择 | 理由 |
|---|--------|------|------|
| 1 | 前后端架构 | 前后端分离 | API Key 仅后端，安全隔离 |
| 2 | 后端框架 | Python FastAPI | LLM 生态丰富，异步/SSE 原生支持好 |
| 3 | 前端框架 | React + TypeScript | 生态成熟，状态管理方案多 |
| 4 | 数据库 | SQLite | 本地应用首选，零配置，单文件 |
| 5 | 大模型 | DeepSeek V4 Flash | OpenAI 兼容，性价比高 |
| 6 | 实时通信 | SSE | 单向推送匹配讨论场景，比 WebSocket 简单 |
| 7 | 发言调度 | 两步管道（调度→发言） | 调度精准，发言贴合人设 |
| 8 | 共识/分歧 | 每次发言后增量更新 | 实时感强 |
| 9 | 主持人机制 | 条件触发（轮次/冷场/跑题/用户） | 自然且可控 |
| 10 | 用户控制 | 轻量干预（暂停/追问/结束） | 有方向盘但不打断讨论流 |

---

## 6. 版本规划

### V1.0（MVP）

- 首页讨论列表
- 阵容生成向导（生成 + 编辑 + 确认）
- 演播厅模式（两步管道 + 主持人条件触发）
- 实时 transcript
- 实时共识与分歧
- 专家状态小窗
- 讨论总结
- 用户控制（暂停/继续/追问/结束）
- 多讨论并行
- SQLite 持久化
- 响应式布局

### V1.1+

- 讨论回放（按时间轴重放 transcript）
- 讨论导出（Markdown / PDF）
- 多 LLM Provider 切换
- 预设话题模板
- 专家人设库（预定义角色模板）
